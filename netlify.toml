[build]
  command = "pip install -r requirements.txt"
  environment = { PYTHON_VERSION = "3.11", PIP_NO_CACHE_DIR = "1" }

[dev]
  command = "uvicorn app:app --host 0.0.0.0 --port 8000"
  targetPort = 8000
  port = 8888
